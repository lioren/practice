void main(){
		int a;
		int b;
		int c;
		a=1;
		b=2;
		printf("assing 1 to local variable a\n");
		printf("assing 2 to local variable b\n");
		printf("the next line will show local variable a\n");
		printf("%d\n",a);
		printf("the next line will show local variable b\n");
		printf("%d\n",b);
		c=a+b;
		printf("the next line show local variable c which is a+b\n");
		printf("%d\n",c);

		c=a-b;
		printf("the next line show local variable c which is a-b\n");
		printf("%d\n",c);
		printf("");
}
